version: '2'

volumes:
  zipline_data_test: {}

services:
  stockbot:
    build:
      context: .
      dockerfile: ./compose/stockbot/Dockerfile-test
    image: cpappalardo/stockbot:test
    volumes:
      - .:/app
      - zipline_data_test:/root/.zipline
